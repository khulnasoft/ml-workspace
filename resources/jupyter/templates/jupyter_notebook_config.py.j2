# Jupyter Notebook Configuration
# This file is automatically generated - DO NOT EDIT MANUALLY

import os
import json
from jupyter_core.paths import jupyter_data_dir

# Load configuration from environment variables
c = get_config()

# Server configuration
c.NotebookApp.ip = '0.0.0.0'
c.NotebookApp.port = int(os.getenv('JUPYTER_PORT', '8090'))
c.NotebookApp.open_browser = False
c.NotebookApp.allow_root = os.getenv('JUPYTER_ALLOW_ROOT', 'true').lower() == 'true'
c.NotebookApp.notebook_dir = os.getenv('JUPYTER_NOTEBOOK_DIR', '/workspace')

# Security settings
c.NotebookApp.token = os.getenv('JUPYTER_TOKEN', '')
c.NotebookApp.password = os.getenv('JUPYTER_PASSWORD', '')
c.NotebookApp.disable_check_xsrf = os.getenv('JUPYTER_DISABLE_XSRF', 'false').lower() == 'true'

# Performance tuning
c.NotebookApp.iopub_msg_rate_limit = int(os.getenv('JUPYTER_IOPUB_MSG_RATE_LIMIT', '100000000'))
c.NotebookApp.iopub_data_rate_limit = int(os.getenv('JUPYTER_IOPUB_DATA_RATE_LIMIT', '2147483647'))

# Extensions
c.NotebookApp.nbserver_extensions = {
    'jupyter_tooling': True,
}

# Load additional configuration files
config_dir = os.path.join(os.path.dirname(__file__), 'config.d')
if os.path.isdir(config_dir):
    for filename in sorted(os.listdir(config_dir)):
        if filename.endswith('.py') and not filename.startswith('_'):
            load_subconfig(os.path.join(config_dir, filename))
